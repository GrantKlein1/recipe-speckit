DB_HOST=localhost
DB_USER=root
DB_PW=
DB_NAME=recipe-speckit_db-test
AUTH_SECRET=test-auth-secret
# Base64-encoded 32-byte key — must match a valid key for crypto tests
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SECRET_KEY=
PORT=3200
NODE_ENV=test
SEQUELIZE_SYNC_ALTER=true
